[project]
name = "travel-advisor"
version = "0.1.0"
description = "An intelligent travel advisor agent with personalized recommendations"
requires-python = ">=3.7"
dependencies = [
    "google-generativeai>=0.3.0",
    "python-dotenv==1.0.0"
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.rye]
managed = true
dev-dependencies = [] 